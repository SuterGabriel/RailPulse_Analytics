# data/

Raw source files are **not versioned** (size and licensing). Download them with the
scripts in `scripts/` into `data/raw/`. Filtered files ready for upload go to
`data/processed/`.

`data/sample/` contains a few hundred rows per source so that the pipeline and the
Streamlit app can be exercised without downloading the full files.
